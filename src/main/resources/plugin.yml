name: ${project.name}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}
api-version: 1.13
authors: [Gimme]
description: ${project.description}
load: STARTUP

commands:
  ores:
    description: Count number of blocks of a specific type in surrounding chunks
    permission: gimmeores.*
    usage: "/ores <block> <chunk distance>"

permissions:
  gimmeores.*:
    description: Allows ores command
    default: op
